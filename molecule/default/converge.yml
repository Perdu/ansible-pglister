---
- name: Converge
  hosts: all

  tasks:
    - name: Update apt cache (on Debian).
      ansible.builtin.apt:
        update_cache: true
      when: ansible_os_family == 'Debian'

- name: Import gitlab-ci test playbook
  ansible.builtin.import_playbook: ../../gitlab-ci.yml
