- name: 'Django: migrate'
  community.general.django_manage:
    command: "migrate"
    app_path: "{{ service_base_path }}/web"
    virtualenv: "{{ service_base_path }}/web/pglister"
  become: true
  become_user: list

- name: 'Django: collectstatic'
  community.general.django_manage:
    command: "collectstatic"
    app_path: "{{ service_base_path }}/web"
    virtualenv: "{{ service_base_path }}/web/pglister"
  become: true
  become_user: list
